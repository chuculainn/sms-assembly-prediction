# Codex任务启动提示词

将下面内容放在软件升级提示词最前面：

```text
本项目的说明书已经放在仓库docs目录中。

开始任何代码修改前，必须先阅读：

1. AGENTS.md
2. docs/REFERENCE_INDEX.md
3. docs/reference/00_data_structure_v25_full.pdf
4. docs/reference/01_data_structure_v25_multipart.pdf

可以使用docs/reference/extracted_text中的文本快速检索，但字段表、矩阵定义和公式必须回到PDF核对。

然后检查现有代码、四零件数据包和测试基线。已经正确实现的功能不要重复推倒重写；保持旧E1、标准数据包和多零件数据包兼容。

完整数据结构说明书用于确定通用对象、字段、主外键和数据治理规则；多零件同步修订稿仅对其明确涉及的多零件章节进行替换和补充。修订稿未涉及的内容继续采用完整说明书。四零件案例只是最小集成基准，不得写死为软件规模上限。

如果说明书、数据包和现有代码存在不一致，请先记录冲突，再采用兼容适配层处理；不得静默修改原始数据，也不得用假数据冒充工程结果。

完成后必须实际运行测试并交付完整软件，不要只给修改建议或补丁片段。
```
