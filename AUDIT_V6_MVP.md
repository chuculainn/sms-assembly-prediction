# V5.4“功能—公式—数据来源—真实性”审计与 V6-MVP 基线

审计日期：2026-07-13

## 结论

V5.4 已形成可运行的研究原型：V2.5/旧 E1 数据包读取、SMS 低阶拟合、法向 LCP、分区等效柔度、简化扩展 LCP、局部切向摩擦投影、KCP、Monte Carlo、物理一致性检查和追溯导出均能执行。

但当前内置数据全部属于合成/占位/演示数据，不能用于证明垂尾翼盒预测精度。软件对规划表中 28 项数值功能的实现程度不一致：核心法向 LCP 和基本 MC 已进入计算主链；接触域生成、Schur/POD、真实阶段状态传递、JSS/J-T、完整验证统计等尚未实现；若干模块仅以等效公式或核函数近似实现，不能按其规划名称宣称为完整求解器。

## 必须立即修正的口径

1. `run_all_stages` 当前对四阶段分别调用 `run_stage`，没有把上一阶段 `StageStateSnapshot`、连接锁定和卸载历史传入下一阶段。应称“分阶段独立求解”，不能称“完整状态传递”。
2. `W_struct` 由数据包 NPZ 读取；软件没有从全阶刚度矩阵自动执行 Schur 凝聚。
3. 扩展 LCP 的接触—定位/夹持/连接耦合项由距离高斯核构造，不是真实 `B_ext/W_struct_ext`。
4. 切向模块是法向求解后的逐点摩擦锥投影，不是法向—切向全耦合 NCP。
5. 粗糙接触是压力相关等效柔度公式，不是 CG-FFT/BEM；表层压陷、夹具、定位器和连接也属于等效近似。
6. KCP 已计算，但贡献台账、JSS/J-T 和防重复闭合尚未计算；现有相关文件主要用于展示或模板读取。
7. `01_DEFAULT_MIN_CASE` 只有 1 个候选点且四阶段均无接触，只证明正式数据结构连通，不证明接触算法或阶段物理。
8. E1 9点/25点算例及验证值均为合成演示，误差不能作为模型验证精度。

## V6-MVP 完成定义

V6-MVP 的目标不是一次完成全功能工业软件，而是形成论文可辩护的最小闭环：

`真实/可追溯 SMS → g0 → q/W → 法向 LCP → 真实阶段状态递推 → KCP → 独立验证 → 误差指标与回退判断`

V6-MVP 必须完成：

- 明确并强制执行数据角色和真实性标签；禁止合成验证值显示为实测。
- 建立真正的 LOCATE→CLAMP→JOIN→RELEASE 状态对象、增量和连接锁定继承。
- 对 `g0/q/W/λ/g/p/w` 建立逐阶段公式追溯和单位/维度检查。
- 实现最小 Schur 凝聚工具或明确把经校核的 CAE `W_struct` 作为外部必需输入，并验证其与全阶静力响应一致。
- 输出 KCP 的 MAE、RMSE、最大误差、容差通过率，并严格只使用 `data_role=VALIDATE` 的独立样本。
- 将 Monte Carlo 每个样本的输入、KCP、求解状态和超域标志完整保留。
- 对接触点加密做总力、接触面积和 KCP 收敛测试。
- 自动测试在 Python 3.11 环境稳定通过，V6 验收跳过项逐项解除。

完整的 28 项审计矩阵、数据来源映射、真实性等级和任务优先级见配套 Excel 审计表。

## 自动测试

审计过程中已修复一项检查错误：扩展 LCP 启用时，物理一致性面板现在使用完整的 `q_ext/W_ext/lambda_ext/g_ext` 重构平衡，不再遗漏定位/夹持/连接反力耦合项。

v5.4.1进一步修复V2.5高级模块组合兼容：正式`MeasurementRecord`可适配为SMS点；原始点不足时保留包内冻结SMS/g0；数值替代`replace`缺少参数时保留包内Cn并告警。三个V2.5数据包均已通过全部高级开关联合回归。

v5.4.2优化物理一致性结果解释：LCP物理可行性、接触形态、KCP产品容差和验证基准分别判定；改变高级模块配置且缺少同配置验证批次时，验证差异只作参考。

运行：

```bash
python scripts/run_automated_tests.py
```

Windows 也可双击 `RUN_AUTOMATED_TESTS.bat`。结果写入 `test_reports/latest/test_report.json` 和 `test_report.csv`。
