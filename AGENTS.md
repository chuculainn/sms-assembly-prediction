# SMS Streamlit 项目执行规则

## 开始工作前

修改数据读取、接触求解、装配拓扑、阶段传递、KCP、Monte Carlo 或验证模块前，必须先阅读：

1. `docs/REFERENCE_INDEX.md`
2. `docs/reference/00_data_structure_v25_full.pdf`
3. `docs/reference/01_data_structure_v25_multipart.pdf`
4. 与当前修改模块有关的辅助说明书

为了便于全文检索，可以先使用 `docs/reference/extracted_text/` 下的文本副本定位内容；出现表格错位、公式缺失或文字不一致时，以对应 PDF 为准。

## 解释优先级

完整数据结构说明书是基础规范；多零件串并联同步修订稿是增量覆盖文件，不是独立的完整说明书。修订稿明确涉及的第2、3、6、7、8、11、14章和附录A中的多零件条款，以修订稿为准；未涉及内容继续遵循完整说明书。

出现说明书、数据包和代码不一致时，按照以下顺序判断：

1. 完整数据结构说明书的通用对象与字段定义；
2. 修订稿生效范围内的多零件替换/补充条款；
3. 四零件数据包实际主外键、向量布局和矩阵维度；
4. 最新软件已有兼容逻辑。

不得静默选择冲突项。应记录冲突位置，优先通过兼容适配层处理，并在最终报告中说明采用的解释。

## 研究与真实性边界

- 软件面向可配置的多零件串联、并联及可能闭环装配结构。
- 四零件案例只是多零件联合求解的最小集成基准，不是规模上限。
- 不得写死零件数、接口数、每接口接触点数、全局向量维数、阶段ID或当前接口名称。
- 合成数据只能用于数值一致性、软件联调和算法自洽检查，不得作为真实工程预测结论。
- 不得使用假代码、空按钮、固定演示结果或预先写死的输出冒充已实现功能。
- 不得修改或覆盖用户原始数据包；运行时结果写入单独输出目录。

## 核心计算约束

- 多接口接触必须使用公共 `VectorLayout` 组成一个全局问题。
- 必须保留 `W_struct` 的跨接口非对角块。
- 正式求解使用 `W_total = W_struct + Cn`。
- 禁止分别求解各接口后再拼接为正式结果。
- 去除跨接口块只能用于诊断对照，必须明确标记为非正式工程结果。
- 阶段链应保持 `LOCATE -> CLAMP -> JOIN -> RELEASE` 的物理语义，同时兼容数据定义的其他阶段ID。
- JOIN形成的连接锁定历史必须在RELEASE中按数据语义继承。

## 修改与验证要求

- 保持旧E1、标准数据包和多零件数据包兼容。
- 修改前运行现有测试并记录基线。
- 修改后保留全部现有测试，并为新功能增加测试。
- 不得通过删除测试、降低判断标准或把失败测试改为跳过来获得通过。
- 最终说明新增功能、修改文件、测试结果、简化实现、未实现功能及数据真实性边界。
