# 04_POSTERIOR_VIRTUAL_SMS_ROLLING_MIN_CASE

Deterministic synthetic integration fixture. The formal route starts from the accepted posterior at MCP_AFTER_ABC, applies five explicit P_D virtual-SMS coefficient vectors from TS301 through TS304, preserves the common contact VectorLayout and one coupled global LCP per solve step, and produces final KCP values plus a predicted-cutoff comparison.

This is not Monte Carlo, does not define a probability distribution or failure probability, and is not engineering validation. `probability_interpretation_allowed=false` and `engineering_claim_allowed=false`.
